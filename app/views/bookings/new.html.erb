<h1>Reserver</h1>
<div id="datepickr-form">
  <%= simple_form_for [@lesson, @booking] do |f| %>
    <div>
      <%= f.input :quantity, label: "nombres de places" %>
    </div>
    <%= f.submit "Réserver" %>
  <% end %>
</div>